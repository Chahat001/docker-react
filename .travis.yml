sudo: required
language: generic

services:
    - docker


before_install:
    - docker build -t chahat/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true chahat/docker-react npm run test
